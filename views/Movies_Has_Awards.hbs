<!-- Refer to README for reference -->

<head>
    <meta charset="utf-8" />
    <title>Members_Has_Movies Subpage </title>
    <link id="styleSheet" href="style.css" rel="stylesheet" type="text/css" />
    <link id="styleSheet" href="style.css" rel="stylesheet" type="text/css" />
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
</head>



<body>
    <header>
    </header>
    <main>
        <section>
            <h2>Awards Movies have won!</h2>
            <table id="moviesHasAwardsTable">
                {{!-- Header section --}}
                <thead>
                    <tr>
                        {{#each data.[0]}}
                        <th>
                            {{@key}}
                        </th>
                        {{/each}}
                        <th>Delete</th>
                    </tr>
                </thead>

                {{!-- Body section --}}
                <tbody>
                    {{!-- For each row, print each ID, movie name, age rating, release date, IMDB rating,
                    Rottent tomatoes rating, # of times rented, sub-genre --}}
                    {{#each data}}
                    <tr>
                        <td>{{this.movie_ID}}</td>
                        <td>{{this.award_ID}}</td>
                    </tr>
                    {{/each}}
                </tbody>
                <table />

                <form method="POST" id="addMovieHasAward">
                    <fieldset>
                        <legend>Add New Award to Movie</legend>
                        <label for="movie">Movie <input type="movie" name="movie" id="movie" autofocus
                                placeholder="Movie" required></label>

                        <label for="award">Award <input type="award" name="award" id="award" placeholder="Award"
                                required></label>

                        <button type="submit">Submit</button>
                    </fieldset>
                </form>

        </section>
    </main>
    <script>
        function editMember(member) {
            alert('Future edits coming!');
        }
    </script>
    <script>
        function deleteMember(member) {
            alert('Future deletion coming!');
        }
    </script>

</body>