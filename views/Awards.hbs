<head>
    <meta charset="utf-8" />
    <title>Awards Sub-page</title>
    <link id="styleSheet" href="/views/css/style.css" rel="stylesheet" type="text/css" />
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
</head>

<headers>

</headers>

<body>
    <header>
        <h1>CREATE Awards</h1>
    </header>
    <main>
        <section>
            <h2>Awards Info</h2>
            <table id="awardTable">
                {{!-- Header section --}}
                <thead>
                    <tr>
                        {{#each data.[0]}}
                        <th>
                            {{@key}}
                        </th>
                        {{/each}}
                    </tr>
                </thead>

                {{!-- Body section --}}
                <tbody>
                    {{!-- For each row, print each ID, movie name, age rating, release date, IMDB rating,
                    Rottent tomatoes rating, # of times rented, sub-genre --}}
                    {{#each data}}
                    <tr data-value="{{this.award_ID}}">
                        <td>{{this.award_ID}}</td>
                        <td>{{this.Award}}</td>
                        <td>{{this.Ceremony}}</td>
                    </tr>
                    {{/each}}
                </tbody>
                <table />

                <h2>Add a new award</h2>
                <form id="add-award-form" method="POST" action="/add-award-form">

                    <label for="input_award">Award:
                        <input type="text" name="input_award" id="input_award" autofocus
                            placeholder="Enter Award Name"></label>

                    <label for="input_ceremony">Ceremony:
                        <input type="text" name="input_ceremony" id="input_ceremony"
                            placeholder="Enter Ceremony"></label>

                    <input type="submit">

        </section>
    </main>
    <script>
        function editMember(member) {
            alert('Future edits coming!');
        }
    </script>
    <script>
        function deleteMember(member) {
            alert('Future deletion coming!');
        }
    </script>

</body>