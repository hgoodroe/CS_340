<head>
    <meta charset="utf-8" />
    <title>Awards Sub-page</title>
    <link id="stylesheet" href="/public_html/CS_340/views/css/style.css" rel="stylesheet" type="text/css" />
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
</head>

<headers>

</headers>

<body>
    <header>
        <!-- <h1>Awards</h1> -->
    </header>
    <main>
        <section>
            <h2>Awards Info</h2>
            <table id="awardTable">
                {{!-- Header section --}}
                <thead>
                    <tr>
                        {{#each data.[0]}}
                        <th>
                            {{@key}}
                        </th>
                        {{/each}}
                        <th>Delete</th>
                    </tr>
                </thead>

                {{!-- Body section --}}
                <tbody>
                    {{!-- For each row, print each ID, movie name, age rating, release date, IMDB rating,
                    Rottent tomatoes rating, # of times rented, sub-genre --}}
                    {{#each data}}
                    <tr data-value="{{this.award_ID}}">
                        <td>{{this.award_ID}}</td>
                        <td>{{this.award}}</td>
                        <td>{{this.ceremony}}</td>
                    </tr>
                    {{/each}}
                </tbody>
                <table />

                <form method="POST" id="addMember">
                    <fieldset>
                        <legend>Add New Award</legend>
                        <label for="award">Award <input type="text" name="award" id="award" autofocus
                                placeholder="Award" required></label>

                        <label for="ceremony">Ceremony <input type="text" name="ceremony" id="ceremony"
                                placeholder="Ceremony" required></label>

                        <button type="submit">Submit </button>
                    </fieldset>
                </form>

        </section>
    </main>
    <script>
        function editMember(member) {
            alert('Future edits coming!');
        }
    </script>
    <script>
        function deleteMember(member) {
            alert('Future deletion coming!');
        }
    </script>

</body>